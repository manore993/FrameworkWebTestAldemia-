# Framework Web Test Aldemia

## Setup

Create venv
```python -m venv .venv```

Activate venv
> depending on operating system 

Install requirements:
```pip install -r requirements.txt```

Install playwright:
```playwright install```

